Restore full context for this repository after /clear or a new session.

Read the following in order:

1. CLAUDE.md — project constitution, golden rules, key commands
2. docs/progress.json — current task, completed steps, next steps, blockers
3. docs/decisions/ — all ADR files (understand WHY things are as they are)
4. Run: `git log --oneline -20` — recent commit history
5. Run: `git status` — any uncommitted work
6. Run: `git diff --stat` — scope of uncommitted changes

Then provide a concise summary:
- What has been done
- What is currently in progress
- What should happen next
- Any blockers or open decisions
- Any golden rules that were recently added (and why)
